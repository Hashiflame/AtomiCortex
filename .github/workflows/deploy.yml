name: Deploy to Cloud VM

on:
  push:
    branches: [master]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11.9'
      
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
      
      - name: Run tests
        run: |
          pip install pytest pytest-asyncio
          pytest tests/ -x --ignore=tests/test_live_feed.py -q

  deploy:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master'
    
    steps:
      - name: Deploy to VM
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.VM_SSH_KEY }}
          script: |
            cd ~/AtomiCortex
            git pull origin master
            source .venv/bin/activate
            pip install -r requirements.txt --quiet
            if [ -x /usr/local/sbin/atomicortex-install-units ]; then
                echo "Running unit installation script..."
                sudo -n /usr/local/sbin/atomicortex-install-units
            else
                echo "WARNING: /usr/local/sbin/atomicortex-install-units not found or not executable."
                echo "Skipping systemd deployment. Manual installation required."
            fi
            echo "Deploy completed at $(date)"
